# Deploy checklist — Fix It Now Admin

Verified bundle includes:

- Pagination on Workers, Customers, Bookings, Reviews, Services, Advertisements, Audit logs
- Plus Jakarta Sans + DM Sans (Google Fonts via `index.html`)
- Vercel long-cache for assets; `index.html` revalidates each deploy

**Vercel env:** `VITE_API_URL` → production Railway API
